# Invalid Lighthouse Attempts

These four JSON files are retained for audit transparency but must not be used for scoring. Each has `runtimeError.code = NO_NAVSTART`, so Chrome did not record a navigation and the category values are not valid measurements.

The valid post-remediation performance report is `../lighthouse-home-mobile-after.json`. Final accessibility changes were verified independently in `../axe-home-mobile-final-after-aria.json`.
